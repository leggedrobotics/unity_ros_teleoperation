[back](/README.md)

# Core
This component contains all the base classes central to system from which other components are expected to inherit, such as the `SensorManager` and `SensorStream` that handle sensor input.